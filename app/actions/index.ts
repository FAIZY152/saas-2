'use server';

export const socialLogin = async (formdata:any) => {
    try {
    const action = formdata.get('action');
    console.log(action);
        
    } catch (error) {
        
    }
    
};
